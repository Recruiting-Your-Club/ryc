spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL  # MySQL 모드로 동작하게 설정
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true