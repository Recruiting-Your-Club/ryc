import React from 'react';
import { line } from './Card.style';

function CardDivider() {
    // prop destruction
    // lib hooks
    // state, ref, querystring hooks
    // form hooks
    // query hooks
    // calculated values
    // effects
    // handlers

    return (
        <>
            <hr css={line} />
        </>
    );
}

export { CardDivider };
