export interface InterviewInformationButtonProps {
    // date: string;
    // title: string;
    label: string;
    onClick?: () => void;
    startTime: string;
    endTime: string;
    isSelected?: boolean;
}
