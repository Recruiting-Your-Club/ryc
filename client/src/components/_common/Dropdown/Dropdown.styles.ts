import { css } from '@emotion/react';
import React from 'react';

export const s_dropdown = css`
    position: relative;
`;

export const s_dropdownTrigger = css``;

export const s_dropdownContent = (offsetX: number, offsetY: number, position: string) => {
    return css``;
};
